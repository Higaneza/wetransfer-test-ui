import React from "react";

import './Navbar.css';

export default function Navbar(props) {
    return (
        <nav className="nav">
            <span className="logo">WeTransfer Test</span>
        </nav>
    );
}